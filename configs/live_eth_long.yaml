# ETH/USDT LONG — paper-live / testnet
# Sin órdenes reales: python cli.py paper-live --config configs/live_eth_long.yaml
# Con órdenes reales en Bybit Testnet: python cli.py paper-live --config configs/live_eth_long.yaml --execute

extends: base_eth_long.yaml

paper_live:
  start_date: "2024-01-01"
  end_date: "today"
  initial_capital: 10000.0
  poll_interval_seconds: 120

risk:
  risk_pct: 0.009
  max_open_positions: 1
  max_drawdown_stop_pct: 0.15
  max_consecutive_losses: 10
